# 項目深度分析報告 - LinkLab

> 分析範圍覆蓋整個 workspace：`linklab/` 主應用、`linklab/admin_dashboard/` 後臺、根 `supabase/`、`linklab/supabase/`、`docs/`、`prd-analysis/`。  
> 分析依據爲靜態代碼審閱、目錄統計、關鍵文件逐個閱讀，以及已執行驗證：`flutter analyze`（主應用 759 個問題，後臺 36 個問題）、`flutter test`（主應用與後臺均在 300 秒內超時，未能確認通過）。

## 1. 項目概覽

### 1.1 項目名稱與核心目標

| 項目項 | 內容 |
|---|---|
| 倉庫名 | `LinkLab` |
| 產品名 | `共感 LinkAble` |
| 一句話目標 | 面向視障、聽障、老年人、肢體障礙者與臨時困難者，提供“AI 先處理、真人志願者兜底”的無障礙互助 Demo 平臺 |
| 核心業務模式 | `AI Agent` 處理約 80% 標準化需求，志願者處理剩餘 20% 的複雜、高風險、情緒化或緊急需求 |
| 當前交付目標 | 競賽型 Demo，而非生產級完整平臺 |
| 關鍵時間節點 | 截止時間爲 `2026-04-30` |

### 1.2 業務場景說明

項目圍繞 `prd.md` 定義的無障礙協助場景構建，重點不是“做一個通用社交 App”，而是做出一條**可演示、可閉環、可講述**的幫助鏈路。當前最成熟的主線是：

`首頁 -> AI 對話 -> AI 解決 或 轉人工 -> 志願者匹配 -> 通話協助 -> 評價沉澱 -> 求助者/志願者中心可回看`

它同時覆蓋以下典型場景：

- OCR 識別與朗讀
- 場景描述、顏色識別
- 實時語音求助
- 異步留言求助
- SOS 緊急廣播
- 求助歷史、常用志願者、無障礙偏好持久化

### 1.3 技術棧、主要框架、語言版本、運行環境

| 維度 | 現狀 |
|---|---|
| 主應用框架 | Flutter |
| 後臺框架 | Flutter Web |
| 後端形態 | Supabase Postgres + Realtime + Edge Functions |
| 主語言 | Dart |
| 後端腳本語言 | TypeScript（Deno Edge Functions） |
| 數據庫腳本 | SQL |
| Dart SDK | `^3.11.4` |
| 主應用狀態管理 | `ChangeNotifier`、單例 service、局部 `setState`，少量 `flutter_riverpod` |
| 後臺狀態管理 | `flutter_bloc` |
| 路由 | 主應用以頁面 push 爲主；後臺使用 `go_router` |
| 本地持久化 | `shared_preferences` |
| 實時能力 | `supabase_flutter` Realtime、`flutter_webrtc` |
| AI/多媒體能力 | OCR、Qwen VL、TTS、STT、Camera、Image Picker |
| 推送相關 | Firebase Messaging、本地通知、Supabase Edge Functions |
| 目標環境 | 移動端 Demo 爲主，Web 構建用於演示交付，後臺主要面向 Web |

### 1.4 項目規模

| 指標 | 統計結果 |
|---|---|
| 非構建產物總文件數 | 約 `439` |
| 核心分析範圍文件數 | `251` |
| 核心分析範圍代碼行數 | 約 `69,655` 行 |
| Dart 文件總數 | `253` |
| 其中生成文件 | `34` |
| 手寫 Dart 文件 | `217` |
| Markdown 文件 | `25` |
| SQL 文件 | `7` |
| TypeScript 文件 | `6` |

主應用 `linklab/lib` 內部體量分佈如下：

| 目錄 | 文件數 | 說明 |
|---|---:|---|
| `services/` | 82 | 業務邏輯與集成層，最重 |
| `models/` | 52 | 數據模型層 |
| `screens/` | 44 | 主要 UI 頁面 |
| `admin/` | 12 | 主應用內嵌後臺殘留/輔助代碼 |
| `demo_data/` | 9 | 演示用靜態數據 |
| `widgets/` | 6 | 可複用組件，尤其無障礙組件 |
| `demo_flow/` | 5 | 演示流程編排 |

---

## 2. 整體架構設計

### 2.1 架構模式判斷

這個項目**不是嚴格的 MVC，也不是嚴格的 Clean Architecture**。更準確的描述是：

> **單倉庫、多應用、多後端定義的 demo-first 分層式單體系統**

它由三塊組成：

```text
LinkLab Monorepo
├─ linklab/                  Flutter 主應用（Demo-first）
│  ├─ UI: screens/widgets
│  ├─ State: AppSessionService + setState + ChangeNotifier
│  ├─ Services: AI / 匹配 / 通話 / 社羣 / 用戶中心
│  └─ Local fallback: SharedPreferences + demo_data
├─ linklab/admin_dashboard/  Flutter Web 運營後臺
│  ├─ Router: go_router
│  ├─ State: flutter_bloc
│  └─ Backend adapter: DemoBackend / SupabaseService
└─ supabase/ + linklab/supabase/
   ├─ SQL migrations
   ├─ RLS policies
   └─ Edge Functions
```

### 2.2 分層結構與職責

| 層級 | 典型目錄 | 職責 |
|---|---|---|
| 展示層 | `screens/`, `pages/`, `widgets/` | 頁面渲染、交互、流程引導、無障礙語義 |
| 會話/狀態層 | `AppSessionService`, `ChangeNotifier`, 局部 `setState` | 登錄狀態、偏好、局部流程狀態 |
| 業務服務層 | `services/` | AI、匹配、通話、SOS、社羣、檔案、異步任務 |
| 數據模型層 | `models/` | JSON/Freezed 數據結構、狀態枚舉、展示模型 |
| 基礎設施層 | `LocalStorage`, `Supabase`, Edge Functions, SharedPreferences | 數據存儲、實時通信、推送、後端適配 |

### 2.3 核心架構特徵

1. **Demo-first 是真實架構原則，不是臨時補丁**  
   `main.dart` 裏明確調用 `AppConfig.setDemoMode()`，並在啓動時加載本地演示數據、初始化本地會話。

2. **主應用以 screen/service 驅動，而非嚴格領域驅動**  
   頁面直接調用 service 單例非常普遍，優點是快，缺點是邊界不穩定。

3. **大量 service 採用“雙軌實現”**  
   常見模式是：
   - 如果 `Supabase.instance.isInitialized == false`，走本地 fallback
   - 否則走真實後端
   這對演示很友好，但也帶來了真實鏈路和演示鏈路長期分叉的問題。

4. **狀態管理不統一**  
   主應用主要用 `ChangeNotifier`、單例 service 和 `setState`，局部引入 `flutter_riverpod`；後臺則採用 `flutter_bloc`。這意味着整個倉庫存在三種並行狀態模型。

5. **無障礙不是點綴，而是顯式設計目標**  
   主題、高對比度、文本縮放、語義標籤、可訪問組件庫都不是裝飾性質，而是主流程的一部分。

### 2.4 關鍵設計決策與模式

| 設計決策/模式 | 體現位置 | 評價 |
|---|---|---|
| 單例服務 | `AppSessionService`, `DemoCallService`, `RealMatchingService` 等 | 易用，但全局耦合高 |
| 觀察者模式 | `ChangeNotifier`, `AnimatedBuilder`, `StreamController` | 在會話和實時狀態中普遍使用 |
| Fallback 模式 | `HelpArchiveService`, `AsyncTaskService`, `FeaturedStoryService` | 非常適合 Demo，但增加分叉維護成本 |
| Freezed/JSON 模型 | 多個 `models/*` | 數據結構層較規範 |
| Facade/Unified Service 嘗試 | `unified_*` 服務 | 方向正確，但收斂不徹底 |
| 文檔驅動開發 | `prd.md`, `DEMO_SCRIPT.md`, `docs/plans/*` | 業務方向明確，落地與文檔部分一致、部分脫節 |

---

## 3. 目錄結構詳解

### 3.1 頂層目錄

| 目錄 | 重要性 | 作用 |
|---|---|---|
| `linklab/` | 最高 | 主應用，整個項目的核心交付物 |
| `linklab/lib/` | 最高 | 主應用所有業務、UI、模型、服務、Demo 流程都在這裏 |
| `linklab/admin_dashboard/` | 高 | 獨立運營後臺，偏演示管理端 |
| `supabase/` | 高 | 根級後端定義，包含主 migrations、RLS、Edge Functions |
| `linklab/supabase/` | 高風險 | 第二套 Supabase migrations，和根 `supabase/` 存在明顯重疊與衝突 |
| `docs/` | 中 | 計劃、對齊文檔 |
| `prd-analysis/` | 中 | 設計分析文檔，體現“理想架構” |
| `.github/` | 低 | 倉庫自動化相關，不是業務核心 |
| `README.md` | 低 | 僅有標題，幾乎不提供實際項目說明 |

### 3.2 `linklab/lib/` 關鍵子目錄

| 目錄 | 作用 | 觀察 |
|---|---|---|
| `screens/` | 業務頁面主體 | 結構清晰，但頁面數量多、風格不完全統一 |
| `services/` | 業務與基礎設施邏輯 | 倉庫最複雜、最容易分叉的部分 |
| `models/` | 統一數據模型 | 是整個項目相對規範的一層 |
| `widgets/` | 複用組件 | 數量不多，但無障礙組件價值高 |
| `demo_flow/` | 演示流程編排 | 是 Demo 閉環的重要“膠水層” |
| `demo_data/` | 本地演示數據 | 爲離線/弱後端演示提供支撐 |
| `core/` | 主題、常量、工具 | 主題與擴展方法集中在這裏 |
| `config/` | 運行配置 | 存在 `app_config.dart` / `api_config.dart` 等 |
| `pages/` | 部分舊頁面/真實功能頁 | 與 `screens/` 有並存與邊界模糊問題 |
| `admin/` | 主應用內後臺殘留 | 與獨立 `admin_dashboard/` 職責重疊 |

### 3.3 關鍵文件

| 文件 | 作用 |
|---|---|
| `prd.md` | 業務與模塊的事實來源 |
| `DEMO_SCRIPT.md` | 演示路線腳本，能反向解釋當前實現爲何偏 demo-first |
| `linklab/lib/main.dart` | 主應用入口，強制演示模式 |
| `linklab/lib/app.dart` | 根 `MaterialApp`，決定首頁/引導/登錄流轉 |
| `linklab/lib/services/app_session_service.dart` | 主應用會話與偏好的本地事實源 |
| `linklab/lib/services/local_storage.dart` | SharedPreferences 持久化抽象 |
| `linklab/lib/screens/home/home_screen.dart` | Demo 首頁與主入口聚合頁 |
| `linklab/lib/screens/ai_chat/demo_ai_chat_screen.dart` | AI 主線入口 |
| `linklab/lib/services/demo_call_service.dart` | Demo 版匹配/通話/SOS |
| `linklab/admin_dashboard/lib/services/demo_backend.dart` | 後臺演示數據核心 |
| `supabase/functions/matching-engine/index.ts` | 真實匹配路徑的後端入口，但與 schema 不一致 |
| `supabase/migrations/*.sql` 與 `linklab/supabase/migrations/*.sql` | 當前最大後端架構衝突來源 |

---

## 4. 依賴與技術棧深度分析

### 4.1 主應用關鍵依賴

| 依賴 | 版本 | 用途 | 評估 |
|---|---:|---|---|
| `supabase_flutter` | `^2.8.4` | 認證、數據庫、Realtime | 關鍵依賴，但代碼與 schema 脫節 |
| `flutter_webrtc` | `^0.12.12` | 實時音視頻 | 平臺兼容複雜，當前實現不穩定 |
| `flutter_riverpod` | `^2.6.1` | 局部狀態管理 | 已引入但沒有全局 `ProviderScope`，存在運行時風險 |
| `firebase_messaging` | `^15.2.5` | 推送 | 需要完整原生配置，當前更多是骨架 |
| `flutter_local_notifications` | `^19.1.0` | 本地通知 | 適合 Demo，但與推送全鏈路未打通 |
| `speech_to_text` / `flutter_tts` | `^7.0.0` / `^4.2.2` | 語音輸入輸出 | 符合產品場景 |
| `image_picker` / `camera` / `image` | 多媒體輸入與處理 | 滿足 OCR/場景識別 | Web 與移動端適配成本高 |
| `freezed_annotation` / `json_annotation` | 數據建模 | 正向資產 |
| `shared_preferences` | `^2.5.2` | Demo 持久化 | 非常適合當前 Demo 路線 |
| `go_router` | `^14.8.0` | 路由 | 主應用使用不充分，後臺更系統化 |

### 4.2 後臺關鍵依賴

| 依賴 | 版本 | 用途 | 評估 |
|---|---:|---|---|
| `flutter_bloc` | `^9.0.0` | 後臺狀態管理 | 比主應用更有組織 |
| `go_router` | `^14.8.0` | 路由管理 | 合理 |
| `supabase_flutter` | `^2.8.0` | 後臺數據源 | 與主應用版本有輕微漂移 |
| `syncfusion_flutter_*` | `^28.1.33` | 表格、圖表、導出 | 適合後臺，但需注意許可治理 |
| `pdf` / `printing` | 導出報表 | 適合運營演示 |

### 4.3 Edge Function 與數據庫依賴

| 組件 | 版本/形態 | 觀察 |
|---|---|---|
| `@supabase/supabase-js` | `2.39.0` | Edge Functions 內部使用 |
| Supabase Config | `Postgres 15` | 根 `supabase/config.toml` 定義本地開發環境 |
| RLS / migrations | SQL | 有兩套定義，當前是系統級風險 |

### 4.4 兼容性與安全風險

1. `supabase_flutter` 在主應用與後臺版本不完全一致，短期影響有限，但會放大排障複雜度。
2. `flutter_riverpod` 已進入代碼，但啓動入口沒有 `ProviderScope`，這不是“風格問題”，而是可達頁面的運行時問題。
3. `flutter_webrtc`、`firebase_messaging`、`flutter_sound` 都屬於平臺敏感依賴，當前倉庫沒有展示出完整的跨平臺收斂策略。
4. `linklab/lib/config/api_config.dart` 被提交進倉庫，而文件頭又明確寫“此文件不應提交到版本控制”，說明配置治理失範。
5. 根 `.gitignore` 極簡，沒有忽略本地敏感配置模板，後續極易引入真實密鑰泄露事故。
6. 資源聲明與實際文件嚴重不一致，運行時容易出現圖片加載失敗。

---

## 5. 核心模塊與文件深度解析

### 5.1 重點文件總覽

| 文件 | 職責 | 核心邏輯 | 主要交互 |
|---|---|---|---|
| `linklab/lib/main.dart` | 主入口 | 強制演示模式、初始化演示數據與會話 | `AppConfig`、`DemoDataLoader`、`AppSessionService` |
| `linklab/lib/app.dart` | 根應用殼 | 根據首次啓動/登錄狀態切換首頁 | `OnboardingScreen`、`LoginScreen`、`MainScreen` |
| `linklab/lib/services/app_session_service.dart` | 會話與偏好中心 | 恢復本地登錄、保存偏好、種子化幫助歷史 | `LocalStorage`、`UserModel`、`AccessibilityPreferences` |
| `linklab/lib/services/local_storage.dart` | Demo 持久化層 | 封裝 SharedPreferences，保存用戶、檔案、任務、聯繫人 | 所有 fallback service |
| `linklab/lib/screens/home/main_screen.dart` | 主導航容器 | 四個底部 Tab：首頁/AI/社羣/我的 | `HomeScreen`、`CommunityScreen`、`ProfileScreen` |
| `linklab/lib/screens/home/home_screen.dart` | 首頁聚合頁 | 聚合最近求助、精選故事、安全狀態、AI/志願者/SOS 入口 | `FeaturedStoryService`、`HelpArchiveService`、`DemoFlowNavigator` |
| `linklab/lib/screens/ai_chat/demo_ai_chat_screen.dart` | Demo AI 主流程 | 處理文本/圖片輸入，調用 AI，識別轉人工或觸發 SOS | `DemoAIService`、`DemoFlowNavigator` |
| `linklab/lib/services/demo_call_service.dart` | Demo 匹配/通話/SOS | 模擬匹配、通話計時、評價回寫、常用志願者沉澱 | `LocalStorage`、`FavoriteVolunteerService` |
| `linklab/lib/services/user_center/help_archive_service.dart` | 求助檔案服務 | 在 Supabase / 本地之間切換，生成統計和歷史記錄 | `LocalStorage`、`help_requests` |
| `linklab/lib/services/user_center/async_task_service.dart` | 異步任務服務 | 留言創建、任務領取、完成、回寫幫助歷史 | `LocalStorage`、`async_tasks` |
| `linklab/lib/services/community/featured_story_service.dart` | 社羣精選故事 | 本地 demo story fallback 與 Supabase 版並存 | `featured_stories`、`story_likes` |
| `linklab/lib/services/real_matching_service.dart` | 真實匹配骨架 | 調 Edge Function、訂閱 Realtime、匹配超時擴圈/轉異步 | `matching-engine`、`help_requests`、`help_request_heartbeats` |
| `linklab/lib/services/webrtc/signaling_service.dart` | WebRTC 信令 | 基於 Supabase Realtime 廣播和房間管理 | `call_rooms`、Realtime channel |
| `linklab/admin_dashboard/lib/services/demo_backend.dart` | 後臺演示後端 | 生成用戶、內容、報表、舉報、統計等內存數據 | 後臺各 BLoC/Screen |
| `supabase/functions/matching-engine/index.ts` | 匹配引擎 | 查詢在線志願者、計算分數、創建 help request、發通知 | `volunteer_profiles`、`help_requests`、`push_notifications` |

### 5.2 深度點評

#### `main.dart`
這是整個項目最關鍵的“立場聲明”文件。它沒有初始化真實 Supabase/Firebase，而是顯式開啓 demo 模式並加載演示數據。這說明當前倉庫最可靠的不是“真實線上鏈路”，而是“本地可演示閉環”。

#### `app.dart`
`LinkLabApp` 通過 `AnimatedBuilder` 監聽 `AppSessionService`，動態切換引導頁、登錄頁和主頁面，並在 `builder` 中統一注入文本縮放。這是無障礙偏好真正影響全局 UI 的樞紐。

#### `app_session_service.dart`
這是主應用最接近“單點事實源”的組件。它負責：
- 恢復本地用戶
- 保存偏好
- 首次啓動判斷
- 維護 demo 幫助歷史種子數據  
它也是當前主應用最值得繼續投資的“穩定核心”。

#### `local_storage.dart`
這個類表面簡單，實際是 Demo 體系的基礎設施底座。幫助歷史、收藏志願者、異步任務、緊急聯繫人都依賴它。主應用的“可閉環感”很大程度來自這裏，而不是來自真實後端。

#### `home_screen.dart`
它是最強的業務聚合頁。頁面初始化時通過 `Future.wait` 併發讀取：
- 最近幫助歷史
- 每日精選故事
- 安全設置
- 緊急聯繫人數量  
同時承載 `AI 對話`、`呼叫志願者`、`異步留言`、`SOS` 四條關鍵入口，是 Demo 敘事上的中心舞臺。

#### `demo_ai_chat_screen.dart`
這個頁面很好地體現了產品分工：
- AI 正常回答時留在對話內
- 識別到“無法/不清楚/志願者”等信號時彈出轉人工
- 識別到 `isEmergency` 或 `sos_triggered` 時彈出 SOS  
它不是單純聊天頁，而是“AI 分流控制器”。

#### `demo_call_service.dart`
這是 demo 主線成功的關鍵文件。它並不真的打電話，但完整模擬了：
- 匹配
- 響鈴
- 接通
- 通話計時
- 掛斷
- 評價
- 回寫幫助檔案
- 累積常用志願者協作次數  
這使得 Demo 在沒有真實 WebRTC 的情況下仍能形成完整結果沉澱。

#### `help_archive_service.dart`
這個服務既能查真實表 `help_requests`，也能把本地 `LocalStorage` 中的數據統一轉換爲 `HelpRequestModel`。它還會生成幫助統計，是求助者中心的關鍵支撐層。

#### `async_task_service.dart`
這是異步留言閉環的核心。它在本地模式下會同時：
- 保存 `async_tasks`
- 同步維護一條 `help_history` 記錄  
因此異步留言不會變成“看不見的隱形邏輯”，而是能在求助者中心被回看。這非常符合 AGENTS.md 的“狀態必須可見”要求。

#### `featured_story_service.dart`
這個服務一方面做了不錯的 fallback 設計，另一方面暴露出明顯一致性問題。典型 bug 是它在 Supabase 路徑中用 `user['role'] != 'admin'` 判斷管理員，但根 schema 的 `users.role` 實際是 `TEXT[]`。

#### `real_matching_service.dart`
這是“未來真實鏈路”的核心骨架，但成熟度明顯低於 demo 線。它依賴：
- HTTP 直接調用 Edge Function
- Realtime 監聽 `help_requests`
- 心跳錶 `help_request_heartbeats`
- 擴圈與異步轉換  
問題在於這套邏輯依賴的表結構與 migrations 並不一致，因此當前更像“設計稿代碼”，不是穩定實現。

#### `signaling_service.dart`
這個文件體現了作者試圖用 Supabase Realtime 做 WebRTC 信令交換，但實現細節存在 SDK 適配錯誤。`subscribe()` 被 `await`，訂閱狀態拿字符串 `'SUBSCRIBED'` 比較，都是明確的實現質量問題。

#### `demo_backend.dart`
後臺最成熟的地方在於：雖然它不真實，但結構完整。它通過純內存數據提供用戶、內容、舉報、報表、統計、故事精選等後臺演示數據，足以支撐一套管理端演示。

#### `matching-engine/index.ts`
這是全倉庫最能暴露“後端定義失控”的文件之一。函數邏輯本身清晰，但它依賴的字段如 `location_lat`、`current_help_count`、`matched_volunteers`、`push_notifications` 在現有數據庫定義中並不完整存在。

---

## 6. 數據流與調用關係

### 6.1 啓動、登錄、會話恢復

```text
main.dart
-> AppConfig.setDemoMode()
-> DemoDataLoader.initialize()
-> AppSessionService.initialize()
   -> LocalStorage.initialize()
   -> 恢復 user_profile / preferences / help_history
   -> 若 history 爲空則 seed demo records
-> runApp(LinkLabApp)
-> app.dart 根據 isLoggedIn / isFirstLaunch 決定入口頁面
```

特點：

- 當前主入口**強制站在 demo 模式**
- 會話恢復依賴本地存儲，而不是遠端認證
- 幫助歷史、偏好、用戶資料都能跨重啓保留

### 6.2 首頁 -> AI 對話 -> 轉人工

```text
HomeScreen
-> 點擊“我需要幫助”/“AI助手”/快捷工具
-> DemoAIChatScreen
-> DemoAIService.process(input)
-> 返回 AIResult
   -> success 且普通結果: 展示文本
   -> need_human: 彈框 -> DemoFlowNavigator.onAIRequestMatching()
   -> isEmergency: 彈框 -> DemoFlowNavigator.onSOSButtonPressed()
```

特點：

- AI 對話頁不是孤立頁，而是主流程決策節點
- `DemoFlowNavigator` 承擔跨頁面編排作用
- 這是當前產品“AI 先處理”的最清晰實現

### 6.3 匹配 -> 通話 -> 評價 -> 檔案沉澱

```text
DemoMatchingFlow.startMatching()
-> DemoMatchingService.startMatching()
-> DemoCallService.startCall()
   -> 選擇 demo volunteer
   -> 創建 help_history 記錄(type=realtime_voice, status=connected)
-> DemoCallScreen
-> hangUp()
   -> 更新 help_history(status=completed)
-> submitSeekerRating()
   -> 回寫 aiResponse/feedback/rating
   -> FavoriteVolunteerService.incrementCooperation()
-> 求助者中心/首頁最近幫助可見
```

特點：

- 這條鏈路是當前倉庫最完整的閉環
- 通話雖爲模擬，但數據沉澱真實發生
- “完成後可回看”這一點做得很好

### 6.4 異步留言 -> 任務隊列 -> 檔案回看

```text
AsyncHelpRequestScreen
-> AsyncTaskService.createTask()
   -> 本地保存 async_tasks
   -> 同步 upsert 到 help_history(type=async)
-> 求助者中心 AsyncRequestsTab 可見
-> 志願者中心 AsyncTasksTab 可領取
-> completeTask()/completeTaskWithVolunteer()
   -> 更新 task
   -> 更新 help_history/status/result
```

特點：

- 這條鏈路比很多 Demo 更成熟，因爲它不是單頁表單，而是形成了求助者側與志願者側的雙視圖
- 真實後端字段命名與本地 JSON 映射並不完全一致，但本地演示閉環可用

### 6.5 SOS 觸發 -> 廣播 -> 聯繫人 -> 響應

```text
HomeScreen SOS 按鈕 / DemoAIChatScreen 緊急判斷
-> DemoFlowNavigator.onSOSButtonPressed()   (Demo 路徑)
或
-> SOSService.triggerSOS()                  (真實路徑骨架)
   -> 獲取位置
   -> 寫入 sos_requests
   -> 調 push-notifier Edge Function
   -> 通知緊急聯繫人
   -> 定時升級廣播
   -> 訂閱 sos_requests 狀態變化
```

特點：

- Demo 路徑更可靠
- 真實路徑設計完整，但數據庫缺表、推送依賴未閉環
- 從產品設計看，SOS 是高價值功能；從工程實現看，它仍偏“骨架層”

---

## 7. 代碼質量與最佳實踐評估

### 7.1 綜合評估表

| 維度 | 評價 | 說明 |
|---|---|---|
| 業務一致性 | 中上 | 主 Demo 流程與 PRD 比較一致 |
| 架構一致性 | 中下 | demo/real、主應用/後臺、雙 Supabase 定義並存 |
| 可讀性 | 中上 | 文件名、目錄名整體清晰，服務命名較直觀 |
| 可維護性 | 中下 | service 過多且並行實現多，邊界開始失控 |
| 測試質量 | 低 | 測試極少且未跑通 |
| 靜態質量 | 低 | 主應用 `flutter analyze` 報 `759` 個問題 |
| 無障礙意識 | 高 | 是項目亮點之一 |
| 安全與配置治理 | 低 | 配置提交策略、認證模型、後端定義都不成熟 |

### 7.2 具體觀察

#### 命名規範
優點：

- 大多數 Dart 文件採用 `snake_case`
- 模型、服務、頁面命名語義明確
- 模塊命名基本貼合 PRD 術語

問題：

- `screens/` 與 `pages/` 職責並不穩定
- `demo_flow/demo_ai_service.dart` 與 `services/demo/demo_ai_service.dart` 等並行實現造成語義重複

#### 錯誤處理
優點：

- 許多 service 在失敗時會 fallback 到空列表、`false`、本地數據，適合 Demo 穩定性

問題：

- 大量 `catch (e) { print(...) }`，沒有統一分級處理
- 真實鏈路錯誤常被“吞掉”或弱化，後期排障困難

#### 日誌
優點：

- 存在 `AppLogger` 這一統一日誌入口嘗試

問題：

- 仍有大量 `print`
- 日誌體系沒有形成跨模塊統一規範

#### 測試覆蓋
現狀：

- `linklab/test/widget_test.dart` 僅是非常基礎的 smoke test
- `linklab/admin_dashboard/test/widget_test.dart` 仍接近 Flutter 默認模板
- `flutter test` 主應用與後臺均超時，無法確認穩定性

結論：

- 對一個多流程、多狀態、多 fallback 的項目而言，測試覆蓋嚴重不足

#### 可讀性與可維護性
優點：

- 多數業務文件本身易讀
- `Freezed/JSON` 模型層增強了結構清晰度
- `AppSessionService` + `LocalStorage` 的思路很穩定

問題：

- 82 個 service 已經出現明顯擴張
- 多套“真實/演示/統一”實現並行，未來會迅速失控
- 後端 schema 與客戶端字段分裂，使得“閱讀代碼不能代表真實系統行爲”

#### 安全性
問題較多：

- 認證主線仍是 demo 模擬
- `VerificationScreen` 等本地邏輯無法視爲真正身份認證
- `api_config.dart` 提交策略錯誤
- 多處硬編碼 `current_user_id`
- 角色字段判斷與數據庫定義不一致

### 7.3 無障礙專項評價

這是本項目最值得肯定的工程特徵之一：

- `AccessibleButton`、`AccessibleScaffold` 等組件抽象明確
- 首頁大按鈕、語義標籤、文本縮放、高對比模式均有落地
- `AppSessionService` 的偏好設置能真正影響全局主題和文本縮放
- 首頁、SOS、AI 聊天等關鍵路徑都考慮了語義描述與可讀性

結論：  
**無障礙意識明顯強於普通競賽項目，且不是隻停留在視覺層。**

---

## 8. 潛在問題與風險清單

| 嚴重級別 | 風險 | 證據 | 影響 |
|---|---|---|---|
| 嚴重 | `Riverpod` 已使用但全局沒有 `ProviderScope` | 可搜索到 `ConsumerWidget/ConsumerStatefulWidget`，但入口 `runApp(const LinkLabApp())` 無 `ProviderScope` | 可達頁面存在運行時異常風險，尤其社區/真實通話頁 |
| 嚴重 | 數據庫 schema 與 Edge Function/客戶端代碼不一致 | `matching-engine/index.ts` 使用 `location_lat`、`matched_volunteers` 等，根 migration 沒有完整定義 | 真實匹配鏈路大概率無法閉環 |
| 嚴重 | 存在兩套 Supabase 定義且互相沖突 | 根 `supabase/` 與 `linklab/supabase/` 都在定義核心表 | 無法明確哪套纔是 source of truth，交付和部署風險極高 |
| 嚴重 | 多個關鍵表在代碼中被引用但 migration 未創建 | `sos_requests`、`featured_stories`、`push_notifications`、`push_tokens`、`push_logs` 等 | 真實 SOS、社羣精選、推送鏈路不可用或不完整 |
| 高 | 資源文件與代碼引用嚴重不一致 | 實際 assets 極少，但代碼引用 `assets/images/volunteer1.png`、`assets/images/training/*`、`assets/avatars/*` 等 | 運行時圖片缺失、演示視覺斷裂 |
| 高 | 多套並行實現導致維護失控 | AI、通話、錄音、後臺、Supabase 定義都有重複版本 | 修改一處不等於系統行爲被真正修復 |
| 高 | 社區模塊大量硬編碼用戶 ID | 多處 `const userId = 'current_user_id'` 和 TODO | 社區/社羣功能不能正確接入真實會話 |
| 高 | 真實通話與信令代碼存在 SDK 適配錯誤 | `signaling_service.dart` 中 `await subscribe()`、狀態字符串比較等 | 真實音視頻鏈路穩定性低 |
| 中高 | 認證流程本質上不是生產認證 | 主應用主線依賴本地會話，不依賴真實 Auth | 真實身份與權限邊界不成立 |
| 中高 | 角色字段語義不一致 | schema 中 `users.role` 爲數組，代碼中有 `user['role'] != 'admin'` | 管理權限判斷可能直接錯誤 |
| 中 | 靜態分析問題數量過多 | 主應用 759 個問題，後臺 36 個問題 | 噪音高，真實缺陷被淹沒 |
| 中 | 測試幾乎缺失 | 核心路徑沒有有效自動化測試 | 迴歸風險高 |
| 中 | 配置治理不規範 | `api_config.dart` 已提交，但註釋要求不入庫 | 後續容易誤提交真實密鑰 |
| 中 | 後臺存在“獨立後臺”與“主應用內 admin 代碼”雙軌 | `linklab/lib/admin/*` 與 `linklab/admin_dashboard/*` | 職責重疊、後期認知成本增加 |

---

## 9. 改進建議優先級

| 優先級 | 改進項 | 理由 | 預期收益 | 難度 |
|---|---|---|---|---|
| P0 | 統一 Supabase schema 的唯一事實來源 | 當前雙套 migrations 是全局最大風險 | 讓真實路徑首次具備可驗證性 | 高 |
| P0 | 修復 `ProviderScope` 缺失與所有可達 Riverpod 頁面 | 這是明確運行時風險 | 直接消除潛在崩潰點 | 低 |
| P0 | 做一次全倉資源清單核對，補齊或統一回退資源 | 當前圖片引用缺失會直接傷害演示 | 穩定 UI 與競賽演示效果 | 中 |
| P1 | 凍結 demo/real 邊界，明確“比賽版只保留哪條主線” | 當前並行實現過多 | 降低維護成本，提升可解釋性 | 中 |
| P1 | 把社區/社羣模塊全部接入 `AppSessionService` 當前用戶 | 當前 `current_user_id` 是明顯殘缺 | 讓社區側狀態與主流程一致 | 中 |
| P1 | 補齊 5 條關鍵閉環測試 | 啓動/登錄、AI->轉人工、通話評價、異步留言、SOS | 防迴歸、提高迭代可信度 | 中 |
| P2 | 清理 `flutter analyze` 高噪音問題 | 759 個問題會掩蓋真正故障 | 提升排障效率和工程可信度 | 中 |
| P2 | 統一日誌與錯誤處理策略，減少 `print` | 真實鏈路排障困難 | 增強調試和線上可診斷性 | 中 |
| P2 | 規範配置與密鑰管理 | `api_config.dart` 當前治理方式不合格 | 降低誤提交與環境混亂風險 | 低 |

### 建議的實際推進順序

```text
第 1 批（立即）
-> ProviderScope
-> 資源補齊
-> current_user_id 清理
-> 明確比賽交付只走 demo 主線

第 2 批（1-3 天）
-> 統一 Supabase source of truth
-> 刪除/隔離重複實現
-> 修復真實匹配/信令最關鍵字段不一致

第 3 批（交付前）
-> 關鍵流程測試
-> analyze 噪音清理
-> 演示腳本與實際資源/頁面逐項對齊
```

---

## 10. 總結與下一步推薦

### 10.1 成熟度評估

| 維度 | 評分 |
|---|---:|
| 產品 Demo 成熟度 | `7/10` |
| 無障礙設計成熟度 | `7.5/10` |
| 工程架構收斂度 | `4/10` |
| 真實後端可落地程度 | `3.5/10` |
| 綜合成熟度 | **`5.5/10`** |

### 10.2 總結判斷

這是一個**產品方向明確、Demo 主線已經成形、無障礙意識明顯優於平均水平**的項目。它最值得肯定的不是“技術堆棧多”，而是已經做出了一條可講述的幫助閉環，尤其是：

- `AI 先處理、失敗再轉人工` 的產品敘事是清楚的
- 本地會話、幫助檔案、異步留言、常用志願者之間形成了真實可見的狀態沉澱
- 首頁、AI 頁、求助者中心之間存在明確的狀態迴流
- 無障礙設計不是貼標籤，而是進入了主題、字體、語義和關鍵交互

但從系統工程角度看，當前項目的最大問題也非常清晰：

- **架構沒有真正收斂**
- **真實鏈路與 demo 鏈路長期並存卻沒有清晰邊界**
- **後端定義發生分裂**
- **部分真實功能代碼已經處於“看起來很多、實際上不可驗證”的狀態**

### 10.3 下一步行動優先級

1. **先保競賽可演示性**  
   立即修復 `ProviderScope`、資源缺失、硬編碼用戶 ID，讓 Demo 主線徹底穩定。

2. **再做後端定義收斂**  
   統一 `supabase/` 與 `linklab/supabase/`，明確唯一 schema 來源，否則真實模式永遠無法驗證。

3. **最後決定真實能力取捨**  
   如果比賽前時間有限，應明確宣佈“比賽版僅保證 demo-first 閉環”，把真實匹配/真實 WebRTC 視爲下一階段，不要讓未閉環代碼拖垮整體可信度。

從競賽視角看，這個項目已經具備“能講清楚”的基礎；從工程視角看，它現在最需要的不是繼續堆功能，而是**做一次強約束下的收斂與減法**。

---  
分析生成於: 2026-04-17 16:01:29 +08:00  
由本地 Codex 深度分析生成
