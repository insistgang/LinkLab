# 共感 LinkAble — 产品需求文档 (PRD)

> **版本**：v1.2（精简版）
> **日期**：2026-04-17
> **产品负责人**：Francis (刘钢)
> **目标平台**：iOS / Android（Flutter 跨平台）
> **竞赛截止**：2026-04-30（两岸大学生创客大赛 · 逢甲赛区）
>
> **v1.2 的关键变化**：功能数从 36 砍到 12（MVP 6 + V1.0 再 6），承认 v1.0/v1.1 的范围过大是**规划错误**。详见 §3 功能范围 与 [附录 D 变更日志](#附录-d-变更日志)。

---

## 目录

- [一、产品概述](#一产品概述)
- [二、设计原则](#二设计原则)
- [三、功能范围](#三功能范围)
- [四、MVP 功能详述（6 个）](#四mvp-功能详述6-个)
- [五、核心流程与状态机](#五核心流程与状态机)
- [六、技术架构](#六技术架构)
- [七、隐私与匿名化](#七隐私与匿名化)
- [八、开发排期](#八开发排期)
- [九、非功能需求](#九非功能需求)
- [十、测试策略](#十测试策略)
- [十一、关键指标 (KPI)](#十一关键指标-kpi)
- [十二、风险与对策](#十二风险与对策)
- [十三、竞赛视角](#十三竞赛视角)
- [附录](#附录)

---

## 一、产品概述

### 1.1 一句话定义

共感 LinkAble 是一款基于 AI Agent 的无障碍社区互助平台。**AI 即时处理 80% 的标准化需求，精准匹配志愿者处理 20% 的复杂需求**，为视障、听障、肢体障碍、老年人及临时困难者提供互助服务。

### 1.2 核心价值主张

| 用户 | 核心价值 |
|---|---|
| 求助者 | 3 秒内获得 AI 响应；复杂问题精准匹配志愿者；身份匿名 |
| 志愿者 | 有成长体系与公益时数认证；不被低价值需求打扰 |
| 平台 | 构建"公益 + B 端"可持续商业模式 |

### 1.3 目标用户画像

| 画像 | 特征 | 核心场景 |
|---|---|---|
| **小王（视障）** | 28 岁，独居，读屏用户 | 辨认衣物颜色、读药品说明、外出导航 |
| **李奶奶（老年）** | 72 岁，不会用智能手机 | 手机操作指导、药品确认、防诈骗 |
| **小陈（听障）** | 22 岁大学生 | 接外卖电话、实时字幕 |
| **小美（台湾视障）** | 19 岁台北学生 | 繁中读屏、跨海峡志愿者连接 |
| **志愿者小李** | 20 岁大学生 | 用碎片时间帮人，积公益时数 |

### 1.4 产品范围边界

**做**：即时现实行动支持（识别 / 导航 / 代沟通）、匿名互助连接、基础激励。

**不做**：付费专业服务（法律/医疗问诊）、电商金融、硬件销售、取代急救体系。

---

## 二、设计原则

> 遇决策冲突回到这里。上位原则优先。

1. **无障碍优先于美学**：读屏用户无法使用的 UI 设计即失败。
2. **AI 优先，人性兜底**：80% 场景 AI 即时响应，永远保留一键转人工。
3. **降低认知负担 > 展示功能**：每页最多 1 个主动作；决策时间 ≤ 3 秒。
4. **隐私默认最强**：最小数据、默认匿名、7 天过期。
5. **安全贯穿架构而非补丁**：举报 / 降权 / 黑名单是架构一部分。
6. **志愿者是用户不是资源**：不压榨排班和时长。
7. **紧急流程必须可离线触发**：SOS 与紧急拨号在断网仍可用。
8. **对错误宽容，对故障透明**：所有失败都有明确提示与降级路径。
9. **优先放弃，不是优先增加** 🆕：功能蔓延是产品头号杀手。新功能必须通过"没它 MVP 能不能演示"的测试。

---

## 三、功能范围

### 3.1 范围的历史（诚实记录）

v1.0 和 v1.1 规划了 36 个功能点，但其中：
- 约 8 个是 **同一 AI 能力被拆成独立功能**（OCR、场景、颜色、钞票、翻译、环境描述、紧急关键词都是 F1 智能对话的意图分支）
- 约 10 个是 **基础设施被误当作产品功能**（推送、认证、紧急联系人、运营后台等）
- 约 12 个是 **价值假设未验证就规划**（积分、徽章、时间线、排班、社群等，没有用户时做这些没意义）

v1.2 砍到 **12 个真正独立功能**（MVP 6 + V1.0 加 6），其余从 PRD 中移除或降级为"实现细节"。

### 3.2 MVP 功能（6 个 · 必做）

> 原则：缺一个就没法构成"AI Agent + 志愿者"的双引擎演示闭环。

| ID | 功能 | 合并前 | 说明 |
|---|---|---|---|
| **F1** | AI Agent 智能对话 | F1+F2+F3+F4+F5+F6+F7+F8 | 单一对话入口，意图分发到 OCR / 场景识别 / 颜色 / 钞票 / 翻译 / 环境 / 紧急词检测。这才是"AI Agent"应有的样子。 |
| **F9** | 志愿者匹配引擎 | F9+F16 | 距离 + 在线 + 信用三维，"老搭档"作为权重参数。 |
| **F11** | 语音通话 | F11+F12 | WebRTC 语音。视频 / 屏幕共享作为"同一通话"的升级模式，V1.0 再加。 |
| **F13** | SOS 紧急呼救 | F13+F32 | 紧急联系人管理合并到 SOS 的实现里。 |
| **F33** | 登录与无障碍偏好 | F33+F17+F28 | 手机号验证码 + 首次引导偏好 + 认证。三合一。 |
| **F36** | 全局无障碍 | F36 | 跨所有页面的技术要求（WCAG AAA、读屏、触摸目标等）。 |

### 3.3 V1.0 追加（6 个 · 赛后 2 个月）

> 原则：MVP 上线后用数据验证"需要这个功能"才做。

| ID | 功能 | 加入理由 |
|---|---|---|
| F10 | 异步留言求助 | 非紧急场景分流，降低志愿者压力 |
| F18 | 等级与积分系统 | 志愿者留存的底层激励 |
| F19 | 技能标签管理 | 匹配精度的关键 |
| F22 | 异步任务队列 | F10 的志愿者端配套 |
| F30 | 双向互评 | 信用体系的基础 |
| F31 | 举报与黑名单 | 安全下限 |

### 3.4 舍弃 / 降级（原 v1.1 有，现移除）

| 原功能 | 处理 |
|---|---|
| F14 帮助档案 | 自动生成，不需要独立功能——用户点历史就是 SQL query |
| F15 安心积分 | 未经验证的留存 gimmick，砍 |
| F20 善意时间线 / F21 徽章 / F23 排班 | 激励装饰品，有志愿者规模后再考虑 |
| F24–F27 社群模块 | 没用户谈什么社群，精选故事硬编码 3 条即可 |
| F29 录音 + AI 检测 | 合规需求，MVP 几百用户无风险，V2.0 再做 |
| F34 消息推送 | 基础设施，归到 F9/F11/F13 的实现里 |
| F35 运营后台 | 用 Supabase Dashboard 替代，V2.0 再单独做 |
| F28 多级认证 | 简化为 F33 的"手机号实名"一档，企业级认证 V1.0+ 做 |

---

## 四、MVP 功能详述（6 个）

### 4.1 F1：AI Agent 智能对话

| 属性 | 说明 |
|---|---|
| **优先级** | P0 |
| **用户故事** | 作为求助者，我用语音 / 文字 / 拍照描述问题，AI 能理解我的意图并直接给出解答；无法解决时能自动为我匹配志愿者。 |

**输入方式**：语音（默认）、文字、拍照、屏幕共享。

**AI 处理流程：**
```
输入 → 意图识别 + 紧急度判断 ──┬── 紧急（救命/晕倒）    → 触发 F13 SOS
                              ├── AI 能处理           → 调用对应能力 → 语音+文字输出
                              └── AI 不能处理         → 转 F9 匹配
```

**意图分类（即原 F2–F8 的统一入口）：**

| 意图 | 触发示例 | 调用能力 | 转人工 |
|---|---|---|---|
| 文字识别（原 F2）| "帮我读这个" | 百度 OCR / PaddleOCR | 识别失败时 |
| 场景描述（原 F3+F7）| "这是什么 / 我面前是什么" | 通义千问 VL / Kimi | 需精确指引时 |
| 颜色识别（原 F4）| "这件衣服什么颜色" | 多模态 VL（VL 直接问即可，无需专用模型）| — |
| 钞票识别（原 F5）| "这是多少钱" | 多模态 VL | — |
| 翻译（原 F6）| "这菜单写的什么" | OCR + 翻译 API | — |
| 导航 | "带我去最近的药店" | 高德地图 API | 室内复杂场景 |
| 药品确认 | "这个药对不对" | AI 初筛 | **必须**转人工 |
| 医疗 / 情感陪伴 | "医生说的什么 / 我害怕" | — | 转对应技能志愿者 |
| 紧急求助（原 F8）| "救命 / 我摔倒了" | 本地关键词词表 | → F13 SOS |

**验收标准：**
- [ ] 9 类意图识别准确率 ≥ 85%
- [ ] 紧急词误识率 ≤ 2%（避免误触 SOS）
- [ ] 首次响应 ≤ 3 秒
- [ ] 连续 3 轮对话上下文保持正确
- [ ] AI 无法处理时 100% 能引导到人工，不出现"死路"
- [ ] 繁体中文 OCR 准确率 ≥ 90%（台湾用户适配）
- [ ] 离线模式（弱网）：基础 OCR + 紧急词检测仍可工作

**异常处理：**
- AI API 超时 > 5s：显示重试，不自动转人工
- AI API 失败：自动降级为本地轻量模型（PaddleOCR Lite）
- 用户连续 3 次未解决：主动"为你转人工"

---

### 4.2 F9：志愿者匹配引擎

| 属性 | 说明 |
|---|---|
| **优先级** | P0 |
| **用户故事** | 作为求助者，AI 解决不了时，系统能在 30 秒内为我匹配到合适的志愿者。 |

**匹配权重公式（简化版 · MVP）：**
```
匹配分 = 0.40×紧急度 + 0.35×距离分 + 0.25×信誉分

归一化：
- 紧急度：normal=0.25, important=0.5, urgent=0.75, emergency=1.0
- 距离分：1 - min(distance_km, 50)/50
- 信誉分：(credit_score - 3.0)/2.0（低于 3.0 直接剔除）
```

> 注：技能匹配 (w) 和历史亲密度 (w5) 留到 V1.0，因为 MVP 阶段志愿者技能标签尚未形成规模数据。

**匹配流程：**
1. AI 处理失败 → 提取紧急度 + 用户位置
2. 查询在线志愿者池 → 按匹配分取 Top 5
3. 并行推送给 Top 5（推送含：求助类型、距离、预估耗时）
4. **第一个接单的志愿者建立连接**（乐观锁抢单）
5. 30 秒无人接单 → 扩大搜索范围；60 秒仍无 → 引导转异步（V1.0 才有 F10，MVP 阶段提示"稍后再试"）

**Race condition 处理**（乐观锁）：
```sql
UPDATE help_requests
SET volunteer_id = :vid, status = 'connected'
WHERE id = :rid AND status = 'matching'
-- 仅第一个 UPDATE 影响 1 行，其他返回 0 行即"已被抢"
```

**验收标准：**
- [ ] 匹配计算耗时 ≤ 500ms（50 个在线志愿者池）
- [ ] 30 秒首次匹配成功率 ≥ 70%（测试阶段 50 名志愿者）
- [ ] 志愿者连续 3 次拒接 / 超时 → 自动降低权重
- [ ] 10 个志愿者同时点接单，只有 1 个成功（并发测试）

---

### 4.3 F11：实时语音通话

| 属性 | 说明 |
|---|---|
| **优先级** | P0 |
| **用户故事** | 被匹配到志愿者后，我能在 5 秒内开始语音通话。 |
| **技术** | WebRTC（Supabase Realtime 信令）|

**核心规则：**
- 建立连接后默认语音（保护隐私）
- 通话录音默认**关闭**（MVP 不做录音，避免隐私合规复杂度 → V1.0 再做）
- 通话时长上限：30 分钟（可延长 1 次）
- 通话中断：任一方掉线 10 秒未恢复 → 状态回到 matching 重新分配

**验收标准：**
- [ ] 信令交换到通话建立 ≤ 5 秒
- [ ] 3G 弱网通话稳定性 ≥ 95%
- [ ] 掉线后重新匹配的状态转换正确（见 §5.2）

---

### 4.4 F13：SOS 紧急呼救

| 属性 | 说明 |
|---|---|
| **优先级** | P0 |
| **用户故事** | 遇到危险时，一键能同时通知附近志愿者 + 紧急联系人，必要时联通 110/120。 |
| **触发** | F1 紧急意图确认 / 连按电源键 3 次 / 大按钮长按 3 秒 |

**流程：**
1. 获取精确位置（GPS + 基站）
2. 广播半径 5 km 内所有在线志愿者（**不走** F9 匹配算法）
3. 同步短信 + Push 通知紧急联系人（含位置链接）
4. 用户授权 → 可选自动拨 120/110
5. 5 分钟无响应 → 扩大至全城；10 分钟仍无 → 强制通知紧急联系人

**紧急联系人管理**（原 F32，合并到此）：
- 设置入口：F33 首次引导 / 设置页
- 最多 3 个，存于 `emergency_contacts` 表
- SOS 触发时按 `priority` 顺序通知

**验收标准：**
- [ ] 触发到首个志愿者收到推送 ≤ 3 秒
- [ ] 紧急联系人短信延迟 ≤ 10 秒
- [ ] 电源键 3 连按即使 App 未启动也可触发（iOS Shortcuts / Android Assistant 集成）
- [ ] 误触撤销窗口：10 秒倒计时

---

### 4.5 F33：登录与无障碍偏好

| 属性 | 说明 |
|---|---|
| **优先级** | P0 |
| **用户故事** | 作为首次使用者，我能在 3 步内完成登录并设置好适合自己的无障碍偏好。 |

**登录：**
- 手机号 + 验证码（无障碍验证码：语音播报 6 位数字）
- 中国大陆：微信一键登录
- 台湾：LINE 登录
- **无密码设计**：每次登录都用验证码，降低视障用户记忆负担

**首次引导（3 步）：**
1. 选择身份：求助者 / 志愿者 / 两者
2. 选择障碍类型（若求助者）/ 技能标签（若志愿者）
3. 设置交互偏好：语音 / 文字优先、字体大小、震动强度、语言（简中 / 繁中 / 英）

**认证**（简化 · MVP）：
- 求助者：手机号 + 实名（微信 / LINE OAuth）
- 志愿者：手机号 + 实名
- 认证专业技能（医疗、手语等）→ V1.0 再做

**账号注销：**
- 设置 → 账号管理 → 注销
- 7 天冷静期，可撤销
- 7 天后硬删除 PII，历史记录匿名化保留
- 符合《个人信息保护法》第 47 条

**验收标准：**
- [ ] 登录 + 引导全流程 ≤ 90 秒
- [ ] 3 步引导可跳过但不强制（可稍后在设置完成）
- [ ] 视障用户用读屏可独立完成注册（真实用户测试通过）

---

### 4.6 F36：全局无障碍适配

| 属性 | 说明 |
|---|---|
| **优先级** | P0（**非功能，是跨页面强制要求**）|

**强制要求（所有页面必须满足）：**

| 维度 | 标准 |
|---|---|
| 语义化 | 所有交互元素有 `Semantics` 标签（Flutter） |
| 对比度 | ≥ 7:1（WCAG 2.1 AAA） |
| 触摸目标 | ≥ 48×48 dp |
| 字体缩放 | 支持系统动态字体，200% 缩放不破版 |
| 焦点顺序 | 逻辑一致，无跳跃 |
| 无时限操作 | 禁止倒计时强制选择 |
| 自动轮播 | 禁用或可暂停 |
| 图片 | 必须有 Alt 文本 |
| 错误提示 | 颜色 + 图标 + 文字三重表达 |
| 读屏支持 | TalkBack / VoiceOver / NVDA 测试通过 |

---

## 五、核心流程与状态机

### 5.1 求助端到端流程

```
┌─────────┐  ┌──────────┐  ┌─────────┐   ┌──────────────┐
│ 打开App │─>│ 点"我需要 │─>│ 选择输入 │──>│ F1 AI Agent  │
│         │  │ 帮助"    │  │方式     │   │ 意图+紧急度  │
└─────────┘  └──────────┘  └─────────┘   └──────┬───────┘
                                                 │
             ┌──── 紧急 ─────┐   ┌─── 非紧急 ────┤
             ▼                   ▼               │
       ┌──────────┐       ┌─────────────┐        │
       │ F13 SOS  │       │ AI 能处理? │        │
       │ 广播+报警│       └──┬──────┬──┘        │
       └──────────┘          │否    │是         │
                             ▼      ▼           │
                    ┌────────┐  ┌──────┐        │
                    │ F9匹配 │  │AI回答│        │
                    └───┬────┘  └──────┘        │
                        │                        │
                        ▼                        │
                 ┌─────────────┐                 │
                 │ F11 语音通话│                 │
                 └─────┬───────┘                 │
                       ▼                         │
                ┌─────────────┐                  │
                │ 结束+评价   │<─────────────────┘
                │(V1.0: F30)  │
                └─────────────┘
```

### 5.2 help_request 状态机

```
created → ai_processing ─┬─> ai_resolved  (AI 直接解决)
                         └─> matching ─┬─> connected → completed
                                       ├─> expired   (60s 无人接 · MVP 终态)
                                       └─> cancelled (用户取消)
connected ─(掉线)─> matching  (重新分配)
```

**状态转换表：**

| 当前 | 事件 | 下一状态 |
|---|---|---|
| created | AI 开始处理 | ai_processing |
| ai_processing | AI 能处理 | ai_resolved |
| ai_processing | AI 不能处理 | matching |
| matching | 志愿者接单（乐观锁）| connected |
| matching | 60s 无人接 | expired |
| matching | 用户取消 | cancelled |
| connected | 通话结束 | completed |
| connected | 掉线 10s 未恢复 | matching（重分配）|

---

## 六、技术架构

### 6.1 整体架构

```
┌─────────────────────────────────────────────────┐
│              客户端 (Flutter)                    │
│  无障碍UI · 语音交互 · 相机 · WebRTC 通话       │
└───────────────────┬─────────────────────────────┘
                    │
┌───────────────────▼─────────────────────────────┐
│              Supabase (BaaS)                     │
│  Auth · PostgreSQL+RLS · Realtime · Storage      │
│  ├─ Edge Functions: 匹配算法 / SOS 广播         │
└───────────────────┬─────────────────────────────┘
                    │
┌───────────────────▼─────────────────────────────┐
│              外部 AI 服务                        │
│  百度OCR · 科大讯飞ASR/TTS · 通义千问VL · 高德  │
└─────────────────────────────────────────────────┘
```

### 6.2 技术选型

| 层 | 选型 | 选型理由 |
|---|---|---|
| 前端 | Flutter 3.x + Dart | 内置 Semantics 无障碍；一套代码双端 |
| 状态管理 | Riverpod 2.x | 类型安全 |
| 后端 | Supabase | PG + Auth + Realtime + Storage 一体；RLS 行级安全 |
| 实时 | WebRTC (flutter_webrtc) | P2P 低延迟 |
| 地图 | 高德地图 SDK | 中国合规 |
| OCR | 百度 OCR + PaddleOCR Lite | 在线高精度 + 离线备选 |
| ASR/TTS | 科大讯飞 | 中文识别率最优 |
| 视觉 | 通义千问 VL / Kimi | 中文场景理解强 |

### 6.3 数据库核心表（MVP 版）

```sql
users (id, phone, name, role[], disability_type[], preferences JSONB,
       locale, status, created_at)

volunteer_profiles (user_id FK, credit_score DECIMAL DEFAULT 5.0,
                    is_online BOOLEAN, location GEOGRAPHY(POINT),
                    response_failures INT DEFAULT 0)

help_requests (id, seeker_id, type, intent, urgency, status,
               ai_response JSONB, volunteer_id, location,
               duration_seconds, created_at, completed_at,
               CONSTRAINT valid_status CHECK (status IN (
                 'created','ai_processing','ai_resolved','matching',
                 'connected','completed','cancelled','expired'
               )))

emergency_contacts (id, user_id FK, name, phone, relationship,
                    priority INT)

virtual_identities (id, user_id FK, help_request_id FK,
                    virtual_code UNIQUE, expires_at)
```

> V1.0 新增表：`async_tasks`、`point_transactions`、`reports`、`historical_pairs`、`skill_certifications`。

### 6.4 关键 API

| Endpoint | 说明 |
|---|---|
| `POST /rpc/submit_request` | 提交求助，进入 F1 AI 流程 |
| `POST /rpc/match_volunteers` | Edge Function，返回 Top 5 |
| `POST /rpc/claim_request` | 志愿者抢单（乐观锁）|
| `POST /rpc/trigger_sos` | SOS 广播入口 |
| Realtime `help_request:{id}` | 匹配 / 通话信令 |

---

## 七、隐私与匿名化

### 7.1 核心原则

求助者与志愿者**默认不交换真实身份**。通信基于 `virtual_identities` 表的代号映射（如 `守护者-M78`）。

### 7.2 技术实现

| 场景 | 实现 |
|---|---|
| 通话身份显示 | 代号 + 脱敏头像 |
| 通话录音 | **MVP 不做**；V1.0 再做（合规复杂度高）|
| 图片上传 | Supabase Storage；URL 签名 30 min 过期；通话结束 2h 后删除 |
| 位置信息 | 精确位置仅在通话期间保留；结束后降为街道级 |
| 历史评价显示 | "与守护者-M78 合作过"，不显示真实名字 |

### 7.3 PII 字段清单

| 字段 | 分类 | 可见范围 |
|---|---|---|
| phone | PII-强 | 用户自己 + 后台审计（脱敏）|
| name | PII-弱 | 脱敏展示 |
| location (精确) | PII-强 | 仅当前通话的志愿者 |
| disability_type | 敏感 | 仅匹配算法使用，不展示给志愿者 |

### 7.4 数据生命周期

```
活跃期：持续保留
图片：求助结束后 2 小时
精确位置：通话结束即清除
注销：7 天冷静期 → 硬删除 PII
```

---

## 八、开发排期

### 8.1 MVP 4 周排期（6 个功能）

| 周 | 任务 | 交付物 |
|---|---|---|
| **W1** | 基础骨架 | Flutter 项目、Supabase 初始化、**F33 登录 + 引导**、**F36 无障碍框架**、首页 UI |
| **W2** | AI 核心 | **F1 智能对话**（OCR + 场景 + 颜色 + 翻译 + 紧急词）、语音交互（讯飞 ASR/TTS）|
| **W3** | 匹配 + 通话 | **F9 匹配引擎**（简化三维权重）、**F11 WebRTC 语音通话**、推送集成 |
| **W4** | 紧急 + 打磨 | **F13 SOS 流程**、Bug 修复、演示视频录制、海报准备 |

### 8.2 MVP 降级路径（进度告急时按顺序舍弃）

| 档位 | 舍弃 | 保留的演示闭环 |
|---|---|---|
| 正常 | — | F1+F9+F11+F13+F33+F36 全部 |
| 降级 1 | 简化 F13 SOS（不接 110/120，仅推送 + 短信）| 其他全部保留 |
| 降级 2 | F9 匹配降为"随机在线志愿者" | F1 + 简单匹配 + F11 通话 |
| 降级 3 | 志愿者端用内置 Mock 账号（不招真实志愿者）| 求助者视角完整 |
| **底线** | 仅 F1 智能对话 + F33 登录 + F36 无障碍 | 这就是演示底线——AI 能力自己就很亮眼 |

**决策点**：每周五晚同步进度，实际完成率 < 70% 即下周执行相应降级。

### 8.3 V1.0 排期（赛后 2 个月）

| 月 | 任务 |
|---|---|
| M1 | F10 异步求助 + F22 志愿者队列 + F30 双向评价 + F31 举报 |
| M2 | F18 等级积分 + F19 技能标签 + 通话录音合规 + 繁中 UI |

---

## 九、非功能需求

### 9.1 性能

| 指标 | 要求 |
|---|---|
| AI 首次响应 | ≤ 3 秒 |
| 匹配耗时 | ≤ 30 秒（首次推送）|
| 通话建立 | ≤ 5 秒 |
| App 冷启动 | ≤ 3 秒 |
| 推送延迟 | ≤ 2 秒 |

### 9.2 安全与隐私

- 符合《个人信息保护法》+《数据安全法》
- 通话录音 V1.0 后再做（MVP 不做避免合规风险）
- 位置信息加密，通话结束模糊化
- 支持数据导出与账号注销（PIPL + GDPR 双合规）

### 9.3 可用性

- 离线：F1 的基础 OCR + 紧急词检测可离线
- 弱网：语音通话降码率
- 设备：Android 8.0+ / iOS 14.0+
- 语言：简中（默认）/ 繁中 / 英

### 9.4 无障碍

- WCAG 2.1 AAA 全面遵循
- 读屏测试（TalkBack + VoiceOver + NVDA）
- 真人视障用户测试（MVP 完成后 3 名）

---

## 十、测试策略

### 10.1 无障碍测试

| 类型 | 工具 | 频率 |
|---|---|---|
| 自动 WCAG 扫描 | accessibility_test（Flutter）| 每次 PR |
| 真人读屏 | TalkBack + VoiceOver | 每 milestone |
| 真实视障用户 | 3 名外部邀请 | MVP 完成后 |

### 10.2 AI 质量测试

| 项 | 测试集 | 通过标准 |
|---|---|---|
| OCR 印刷体 | 500 张 | ≥ 95% |
| 场景识别 | 300 张 | 主要物体召回 ≥ 85% |
| 意图识别 | 2000 条 | ≥ 85% |
| 紧急词识别 | 100 条 | 召回 ≥ 95%，误识 ≤ 2% |

### 10.3 性能 & 并发测试

- 50 并发求助下匹配 P95 ≤ 30 秒
- 10 志愿者并发抢单，只 1 成功（乐观锁）
- 3G 通话不掉线率 ≥ 95%

---

## 十一、关键指标 (KPI)

### 11.1 核心漏斗

```
打开 App (100%)
  ↓ 30%
发起求助
  ↓ 95%
AI 解决 OR 匹配成功
  ↓ 85%
通话 / 回覆完成
  ↓ 60%
给出评分
  ↓ 80%
5 星好评
```

### 11.2 MVP 阶段关注（其他指标留给 V1.0）

| 指标 | MVP 目标 |
|---|---|
| AI 解决率 | > 60% |
| 求助响应率（5 分钟内接听）| > 70% |
| 5 星好评率 | > 80% |
| 虚惊 SOS | < 10% |

---

## 十二、风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| **功能蔓延（头号风险）** 🆕 | MVP 无法按期交付 | 每周回到 §2 原则 #9，严守 6 功能边界 |
| 冷启动志愿者不足 | 求助无人响应 | 种子用户（10–20 名核心）+ 运营模拟 + 高校合作 |
| AI 误判紧急度 | 误触或漏触 SOS | 二次确认 + 10 秒撤销窗口 + 紧急词库持续优化 |
| 隐私泄露 | 法律风险 | 端到端加密 + 数据最小化 + 7 天过期 + 合规审计 |
| API 成本高 | 运营不可持续 | 分级调用（简单任务用轻量模型）+ 缓存 |
| 恶意用户 | 骚扰志愿者 | MVP 简单举报；V1.0 加信用分 + 设备指纹 |
| 两岸合规差异 | 台湾数据跨境 | 台湾节点独立化（V1.0+ 处理）|

---

## 十三、竞赛视角

### 13.1 评审维度预判

| 维度 | 占比（估）| 我们的得分点 |
|---|---|---|
| 创新性 | 25% | AI Agent 第一响应 + 全障碍覆盖 |
| 可行性 | 20% | **12 个功能的精简 MVP（不画饼）** |
| 社会影响 | 20% | SDG 3/10/11/17；8591 万残疾人受益 |
| 技术实现 | 15% | Flutter + Supabase + 多模态 AI |
| 展示效果 | 10% | 演示视频 + 海报 + 答辩 |
| 永续性 | 10% | B 端付费 + 公益时数 |

### 13.2 3 分钟演示脚本

| 时间 | 动作 | 亮点 |
|---|---|---|
| 0:00–0:20 | 打开 App，大按钮吸睛 | F36 无障碍第一印象 |
| 0:20–0:50 | 语音求助"帮我读药品盒"→ AI 识别朗读 | F1 AI 能力 1 |
| 0:50–1:30 | "我面前是什么"→ 场景描述 | F1 AI 能力 2 |
| 1:30–2:10 | 复杂需求 → F9 匹配 → F11 通话 | 双引擎展示 |
| 2:10–2:40 | F13 SOS 演示（Mock 模式） | 社会价值 |
| 2:40–3:00 | 未来蓝图（V1.0 功能 + 跨海峡）| 永续性 |

### 13.3 答辩 Q&A

**Q：只有 6 个功能会不会太少？**
A：MVP 就是要少。Be My Eyes 做了 8 年才 3 个核心功能。我们的 6 个已经能完整演示"AI 优先 + 人工兜底"的双引擎，且每个都是可实测跑通的，不是 PPT。

**Q：和 Be My Eyes 有什么区别？**
A：Be My Eyes 只做视障 + 只有视频。我们做全障碍 + AI 第一响应 + 三层连接（AI / 异步 / 实时）。

**Q：志愿者不够怎么办？**
A：冷启动靠种子用户 + 高校合作；算法上 MVP 不够时至少 AI 能解决 80%。

**Q：商业模式？**
A：公益永久免费；B 端（银行 / 医院 / 政务无障碍客服）按接入点付费；2027 年营收覆盖成本。

**Q：为什么砍掉积分 / 社群？**
A：诚实：没用户谈社群是伪需求。MVP 先证明"求助能被解决"，再考虑"怎么让用户回来"。

---

## 附录

### 附录 A：成本估算

**MVP 阶段（月）：**

| 服务 | 费用 | 说明 |
|---|---|---|
| Supabase | ¥0 / ¥175 | Free tier 足够 MVP |
| 百度 OCR | ¥0 | 免费 50,000 次 / 月 |
| 讯飞 ASR | ¥0–200 | 免费额度 + 按量 |
| 通义千问 VL | ¥100–300 | 按 token |
| 高德 / FCM | ¥0 | 免费 |
| **合计** | **¥0–675/月** | — |

**扩展期（1000 DAU / 月）：** ¥8,700–11,000 / 月（约 ¥8–11 / DAU）

**扩展期（10,000 DAU / 月）：** ¥8–10 万 / 月，需 B 端付费 + 公益基金支撑

### 附录 B：MVP 页面清单（Figma）

| 页面 | 对应功能 |
|---|---|
| 注册引导页（3 步）| F33 |
| 首页（大按钮 + 快捷工具）| F1 入口 |
| AI 对话页（ChatBot 式）| F1 |
| 匹配等待页 | F9 |
| 语音通话页 | F11 |
| SOS 触发页（全屏红色 + 倒计时）| F13 |
| 设置页（无障碍偏好 + 紧急联系人）| F33 延续 |
| 帮助完成页（MVP 简版评价）| — |

### 附录 C：术语表

| 术语 | 含义 |
|---|---|
| AI Agent | 第一响应的 AI，包含意图识别、紧急度判断、能力调度 |
| 双引擎 | AI 处理（80%）+ 人类志愿者（20%）协同 |
| SOS | 紧急呼救广播机制（F13）|
| RLS | Row Level Security（Supabase 的行级安全）|
| 代号 | 虚拟身份标识（如"守护者-M78"），匿名交互用 |
| AC | Acceptance Criteria，验收标准 |
| WCAG | Web Content Accessibility Guidelines |

### 附录 D：变更日志

| 版本 | 日期 | 主要变更 |
|---|---|---|
| **v1.2** | 2026-04-17 | **重大范围调整**：功能从 36 砍到 12（MVP 6 + V1.0 加 6）<br>① F1 AI 对话吸收原 F2/F3/F4/F5/F6/F7/F8<br>② F33 登录吸收原 F17/F28<br>③ F13 SOS 吸收原 F32 紧急联系人<br>④ 砍掉 F14/F15/F20/F21/F23/F24–F27/F29/F34/F35 等未验证功能<br>⑤ MVP 排期重排，降级路径更清晰<br>⑥ 新增"功能蔓延"为头号风险<br>⑦ 设计原则加 #9「优先放弃不是优先增加」 |
| v1.1 | 2026-04-17 | 补齐 P0 验收标准、状态机、隐私设计、测试策略、竞赛视角 |
| v1.0 | 2026-04-10 | 初版 PRD，36 个功能点（范围过大，v1.2 修正）|

---

*文档持续迭代。PRD 的第一性原理：说明**要做什么**和**不做什么**，同等重要。*
